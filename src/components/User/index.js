import React from 'react'
import PropTypes from 'prop-types'

import { ContentContainer, PageSubTitle, ErrorMessage } from 'styles/sharedStyles'

const User = ({}) => (
  <ContentContainer>
    <PageSubTitle>{'User'}</PageSubTitle>
  </ContentContainer>
)

export default User
