import React from 'react'

import { ContentContainer, PageTitle, PageSubTitle } from 'styles/sharedStyles'

const Logout = props => (
  <ContentContainer>
    <PageTitle>{'Logout'}</PageTitle>
    <PageSubTitle>{'You are now logged out'}</PageSubTitle>
  </ContentContainer>
)

export default Logout
